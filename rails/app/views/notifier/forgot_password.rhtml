Dear <%= @name %>,

Someone (probably you, from IP address <%= @remote_ip %>)
requested a password reset.

To reset your password just follow the link where you can define a new
password:
<%= @link %>

