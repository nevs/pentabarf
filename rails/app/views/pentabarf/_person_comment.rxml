xml = Builder::XmlMarkup.new(:indent=>1)

xml.div(:id=>'content-comment') do
  xml.img( :src=>url_for(:controller=>'image',:action=>:person,:id=>"#{@person.person_id}-128x128"),:style=>'float: right;')
  xml.fieldset do
    xml.legend local('table::person::remark')
    xml.table do
      xml.tr do
        xml.td do
          xml << text_area_tag('person[remark]', @person.remark, {:rows => 5, :cols => 60, :tabindex => 0})
        end
      end
    end
  end
end

