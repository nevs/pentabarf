xml = Builder::XmlMarkup.new(:indent=>1)

xml.div(:id=>'content-simple') do
  xml.fieldset do
    xml.input(:id=>'find_event',:size=>40)
  end
  xml << observe_field( :find_event, :frequency => 1, :update => :results, :url => { :action => :search_event} )
end

