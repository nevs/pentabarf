xml = Builder::XmlMarkup.new(:indent=>1)

xml.div(:id=>'content-comment') do
  xml.img( :src=>url_for(:controller=>'image',:action=>:conference,:id=>"#{@conference.conference_id}-128x128"),:style=>'float: right;')
  xml.fieldset do
    xml.legend local('table::conference::remark')
    xml.table do
      xml.tr do
        xml.td do
          xml << text_area_tag('conference[remark]', @conference.remark, {:rows => 5, :cols => 60, :tabindex => 0})
        end
      end
    end
  end
end

