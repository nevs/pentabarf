xml = Builder::XmlMarkup.new(:indent=>1)

xml.div({:id=>:data}) do
  xml.ul do
    ReportController::REPORTS.each do | report |
      xml.li do
        xml.a( local(report),{:href=>url_for({:action=>report})})
      end
    end
  end
end
