<div id="event-index" class="section">
  <table class="event-index">
    <thead>
      <tr>
        <th class="event">Event</th>
        <th class="speaker">Speaker</th>
      </tr>
    </thead>
    <tbody>
      <% @events.each_unique(:event_id) do |event | %>
      <tr>
        <td class="event">
          <a href="<%= url_for(:action=>:event,:id=>event.event_id) %>"><img src="<%= event_image(event.event_id, 48) %>" /></a>
          <p class="title"><a href="<%= url_for(:action=>:event,:id=>event.event_id) %>"><%= h(event.title) %></a></p>
          <p class="subtitle"><a href="<%= url_for(:action=>:event,:id=>event.event_id) %>"><%= h(event.subtitle) %></a></p>
          <p>
            <a href="<%= url_for(:action=>:event,:id=>event.event_id) %>">
              <strong><[table::event::event_type]></strong>: <span class="type"><%= h(event.event_type) %></span><br/>
              <strong><[table::event::conference_track]></strong>: <span class="track"><%= h(event.conference_track) %></span><br/>
              <strong><[table::event::language]></strong>: <span class="language"><%= h(event.language) %></span><br/>
            </a>
          </p>
        </td>
        <td class="speakers">
          <ul class="speakers">
            <% @events.each_by_value({:event_id=>event.event_id}) do | speaker | %>
              <li><a href="<%= url_for(:action=>:speaker,:id=>speaker.person_id) %>"><%= h(speaker.name) %></a></li>
            <% end %>
          </ul>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
