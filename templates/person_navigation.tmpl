    <div class="object_navigation">
      <form id="form_person_selector" class="menu-item" method="post" action="{VIEW_URL}">
        <select name="select_person" onchange="submit()">
          <patTemplate:tmpl name="person_list" src="select.tmpl" parse="on" />
        </select>
      </form>
    </div>

