
-- schema for conflict system related stuff
CREATE SCHEMA conflict;

-- schema where all master tables reside
CREATE SCHEMA master;

-- schema where all logging tables reside
CREATE SCHEMA logging;

